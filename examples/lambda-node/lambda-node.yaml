# lambda-node.yaml

# 字段结构与 lambda.yaml 完全一致, 仅 compiler/variant 值为 Node.js 语义。
# 上层工程可以用同一套解析代码读取 lambda.yaml 或 lambda-node.yaml。

dynamic:
  environment:
    toolchain:
      os: darwin15.7.3
      arch: amd64v1
      compiler: node25.8.0
      variant: bundle
    warehouse:
      local: /tmp/dynamic-node-lambda-warehouse
      remote: s3://dynamic-loader-code-255491288557
  package:
    namespace: hotscripts
    defaultVersion: v1
    preload:
      - package: hello
        version: v1